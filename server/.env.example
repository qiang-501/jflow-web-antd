# Application
PORT=3000
NODE_ENV=development

# Database
DB_HOST=10.86.131.84
DB_PORT=80
DB_USERNAME=postgres
DB_PASSWORD=
DB_DATABASE=jflow

# JWT
JWT_SECRET=change-this-secret-key
# JWT token 有效期：可使用 s (秒), m (分钟), h (小时), d (天)
# 示例：60s, 30m, 2h, 7d
JWT_EXPIRES_IN=2h

# CORS
CORS_ORIGIN=http://localhost:4200
